import { StyleSheet } from 'react-native'

export const authStyles = StyleSheet.create({
    marginBottom: {
        marginBottom: 20,
    },
    marginBottomSmall: {
        marginBottom: 5,
    },
    errorMessage: {
        textAlign: 'center',
    },
})
